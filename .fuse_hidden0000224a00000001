import pickle
import os

pickle_in = open(os.getcwd() + "/doc_vectors.pickle","rb")
vectors = pickle.load(pickle_in)
print(len(vectors[0]))
flag = 0
i = 0
for vector in vectors:
    if(len(vector) != len(vectors[1]) and i!=0):
        flag = 1
        print(i)
    i = i+1

print(flag)
